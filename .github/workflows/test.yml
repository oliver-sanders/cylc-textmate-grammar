name: test

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  # run Cylc tests
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Setup Node.js environment
      uses: actions/setup-node@v4
      with:
        node-version: 'lts/*'

    - name: Install (dev) dependencies
      run: npm install

    - name: Style test
      run: npm run lint

    - name: Build
      run: |
        mv cylc.tmLanguage.json tmLang_copy.json
        npm run build

    - name: Check grammar file matches build output
      run: diff -Z cylc.tmLanguage.json tmLang_copy.json

    - name: Unit tests
      run: npm test

  # run Linguist tests
  linguist:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: |
          echo "
          ref: ${{ github.ref }}
          repository: ${{ github.repository }}
          "

      - name: checkout
        uses: actions/checkout@v1
        with:
          repository: github-linguist/linguist
          ref: master

      - name: Patch
        run: |
          line='git submodule add -f "$url" "$path"'
          sed -i "s|$line|$line -b ${{ github.ref }}|" linguist/script/add-grammar

      - name: Install
        run: |
          sudo gem install bundler

      - name: Add Cylc
        run: |
          set +x
          linguist/script/add-grammar https://github.com/${{ github.repository }}
          linguist/script/update-ids
